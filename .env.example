# Absolute URL of the externally-hosted contact form endpoint.
# The contact form on /contact POSTs JSON ({ name, email, phone, message })
# to this URL — it is not served from this repo/deployment.
PUBLIC_CONTACT_API_URL=
